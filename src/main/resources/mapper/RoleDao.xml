<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pzh.zp.dao.RoleDao">

    <resultMap type="com.pzh.zp.entity.Role" id="RoleMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="rName" column="r_name" jdbcType="VARCHAR"/>
        <result property="description" column="description" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="RoleMap">
        select
          id, r_name, description, create_time, status
        from pzh_meeting_management.role
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="RoleMap">
        select
          id, r_name, description, create_time, status
        from pzh_meeting_management.role
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="RoleMap">
        select
        id, r_name, description, create_time, status
        from pzh_meeting_management.role
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="rName != null and rName != ''">
                and r_name = #{rName}
            </if>
            <if test="description != null and description != ''">
                and description = #{description}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into pzh_meeting_management.role(r_name, description, create_time, status)
        values (#{rName}, #{description}, #{createTime}, #{status})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into pzh_meeting_management.role(r_name, description, create_time, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.rName}, #{entity.description}, #{entity.createTime}, #{entity.status})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into pzh_meeting_management.role(r_name, description, create_time, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.rName}, #{entity.description}, #{entity.createTime}, #{entity.status})
        </foreach>
        on duplicate key update
        r_name = values(r_name) , description = values(description) , create_time = values(create_time) , status =
        values(status)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update pzh_meeting_management.role
        <set>
            <if test="rName != null and rName != ''">
                r_name = #{rName},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from pzh_meeting_management.role where id = #{id}
    </delete>

</mapper>