<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pzh.zp.dao.FileuploadDao">

    <resultMap type="com.pzh.zp.entity.Fileupload" id="FileuploadMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="fileName" column="file_name" jdbcType="VARCHAR"/>
        <result property="fileUrl" column="file_url" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="FileuploadMap">
        select id,
               file_name,
               file_url,
               create_time
        from pzh_meeting_management.fileupload
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="FileuploadMap">
        select id,
               file_name,
               file_url,
               create_time
        from pzh_meeting_management.fileupload limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="FileuploadMap">
        select
        id, file_name, file_url, create_time
        from pzh_meeting_management.fileupload
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="fileName != null and fileName != ''">
                and file_name = #{fileName}
            </if>
            <if test="fileUrl != null and fileUrl != ''">
                and file_url = #{fileUrl}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into pzh_meeting_management.fileupload(file_name, file_url, create_time)
        values (#{fileName}, #{fileUrl}, #{createTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into pzh_meeting_management.fileupload(file_name, file_url, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.fileName}, #{entity.fileUrl}, #{entity.createTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into pzh_meeting_management.fileupload(file_name, file_url, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.fileName}, #{entity.fileUrl}, #{entity.createTime})
        </foreach>
        on duplicate key update
        file_name = values(file_name) , file_url = values(file_url) , create_time = values(create_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update pzh_meeting_management.fileupload
        <set>
            <if test="fileName != null and fileName != ''">
                file_name = #{fileName},
            </if>
            <if test="fileUrl != null and fileUrl != ''">
                file_url = #{fileUrl},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from pzh_meeting_management.fileupload
        where id = #{id}
    </delete>

    <select id="fuzzyFile" parameterType="String" resultMap="FileuploadMap">
        select id,
               file_name,
               file_url,
               create_time
            from pzh_meeting_management.fileupload
        <where>
            <if test="fileName != null and fileName != ''">
                file_name like concat('%',#{fileName},'%')
            </if>
        </where>
    </select>

</mapper>

