<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pzh.zp.dao.ConferenceDao">

    <resultMap type="com.pzh.zp.entity.Conference" id="ConferenceMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="startTime" column="start_time" jdbcType="TIMESTAMP"/>
        <result property="endTime" column="end_time" jdbcType="TIMESTAMP"/>
        <result property="place" column="place" jdbcType="VARCHAR"/>
        <result property="host" column="host" jdbcType="VARCHAR"/>
        <result property="desc" column="desc" jdbcType="VARCHAR"/>
        <result property="ongoing" column="ongoing" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ConferenceMap">
        select
          id, name, start_time, end_time, place, host, `desc`, ongoing
        from pzh_meeting_management.conference
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ConferenceMap">
        select
          id, name, start_time, end_time, place, host, `desc`, ongoing
        from pzh_meeting_management.conference
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="ConferenceMap">
        select
        id, name, start_time, end_time, place, host, `desc`, ongoing
        from pzh_meeting_management.conference
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="startTime != null">
                and start_time = #{startTime}
            </if>
            <if test="endTime != null">
                and end_time = #{endTime}
            </if>
            <if test="place != null and place != ''">
                and place = #{place}
            </if>
            <if test="host != null and host != ''">
                and host = #{host}
            </if>
            <if test="desc != null">
                and `desc` = #{desc}
            </if>
            <if test="ongoing != null">
                and ongoing = #{ongoing}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into pzh_meeting_management.conference(name, start_time, end_time, place, host, `desc`, ongoing)
        values (#{name}, #{startTime}, #{endTime}, #{place}, #{host}, #{desc}, #{ongoing})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into pzh_meeting_management.conference(name, start_time, end_time, place, host, `desc`, ongoing)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.startTime}, #{entity.endTime}, #{entity.place}, #{entity.host}, #{entity.desc}, #{entity.ongoing})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into pzh_meeting_management.conference(name, start_time, end_time, place, host ,`desc`, ongoing)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.startTime}, #{entity.endTime}, #{entity.place}, #{entity.host}, #{entity.desc}, #{entity.ongoing})
        </foreach>
        on duplicate key update
        name = values(name) , start_time = values(start_time) , end_time = values(end_time) , place = values(place) ,
        host = values(host), `desc` = values(desc), ongoing = values(ongoing)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update pzh_meeting_management.conference
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="startTime != null">
                start_time = #{startTime},
            </if>
            <if test="endTime != null">
                end_time = #{endTime},
            </if>
            <if test="place != null and place != ''">
                place = #{place},
            </if>
            <if test="host != null and host != ''">
                host = #{host},
            </if>
            <if test="desc !=null and desc != ''">
                `desc` = #{desc},
            </if>
            <if test="ongoing != null and ongoing != ''">
                ongoing = #{ongoing}
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from pzh_meeting_management.conference where id = #{id}
    </delete>

    <!--模糊查询  -->
    <select id="fuzzySearch" resultMap="ConferenceMap" parameterType="String">
        select id, name, start_time, end_time, place, host, `desc`, ongoing
        from pzh_meeting_management.conference
        <where>
            <if test="name!=null and name!=''">
                name like concat('%',#{name},'%')
            </if>
            <if test="place!=null and place!=''">
                or place like concat('%',#{place},'%')
            </if>
            <if test="host!=null and host!=''">
                or host like concat('%',#{host},'%')
            </if>
        </where>
    </select>

    <!-- 查找所有的会议名   -->
    <select id="queryAllName" resultType="String">
        select name
        from pzh_meeting_management.conference
    </select>

    <select id="queryIdByName" parameterType="String" resultType="Integer">
        select id
        from pzh_meeting_management.conference
        <where>
            <if test="name!=null and name!=''">
                name = #{name}
            </if>
        </where>
    </select>

    <select id="counting" resultType="Integer">
        select count(1)
        from pzh_meeting_management.conference;
    </select>

    <!--通过地点-->
    <select id="findConferenceByplace" resultMap="ConferenceMap">
        select *
        from pzh_meeting_management.conference
        <where>
            <if test="place!=null and place!=''">
                 place = #{place}
            </if>
        </where>
    </select>
</mapper>