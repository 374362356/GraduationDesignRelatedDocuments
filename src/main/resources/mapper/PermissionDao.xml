<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pzh.zp.dao.PermissionDao">

    <resultMap type="com.pzh.zp.entity.Permission" id="PermissionMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="pName" column="p_name" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="url" column="url" jdbcType="VARCHAR"/>
        <result property="path" column="path" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="PermissionMap">
        select id,
               p_name,
               create_time,
               status,
               url,
               path
        from pzh_meeting_management.permission
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="PermissionMap">
        select id,
               p_name,
               create_time,
               status,
               url,
               path
        from pzh_meeting_management.permission limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="PermissionMap">
        select
        id, p_name, create_time, status, url, path
        from pzh_meeting_management.permission
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="pName != null and pName != ''">
                and p_name = #{pName}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="url != null and url != ''">
                and url = #{url}
            </if>
            <if test="path != null and path != ''">
                and path = #{path}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into pzh_meeting_management.permission(p_name, create_time, status, url, path)
        values (#{pName}, #{createTime}, #{status}, #{url}, #{path})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into pzh_meeting_management.permission(p_name, create_time, status, url, path)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.pName}, #{entity.createTime}, #{entity.status}, #{entity.url}, #{entity.path})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into pzh_meeting_management.permission(p_name, create_time, status, url, path)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.pName}, #{entity.createTime}, #{entity.status}, #{entity.url}, #{entity.path})
        </foreach>
        on duplicate key update
        p_name = values(p_name) , create_time = values(create_time) , status = values(status) , url = values(url) , path
        = values(path)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update pzh_meeting_management.permission
        <set>
            <if test="pName != null and pName != ''">
                p_name = #{pName},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="url != null and url != ''">
                url = #{url},
            </if>
            <if test="path != null and path != ''">
                path = #{path},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from pzh_meeting_management.permission
        where id = #{id}
    </delete>

    <select id="findByUserId" parameterType="Integer" resultMap="PermissionMap">
        select p.*
        from user u
         LEFT JOIN role_user ru on u.id= ru.user_id
         LEFT JOIN role r on ru.role_id=r.id
         LEFT JOIN role_permission rp on rp.role_id=r.id
         LEFT JOIN permission p on p.id =rp.permission_id
        where u.id=#{id}
    </select>
    
    <select id="queryFuzzy" parameterType="String" resultMap="PermissionMap">
        select *
        from  pzh_meeting_management.permission
        <where>
            <if test="pName != null and pName != ''">
                p_name like concat('%',#{pName},'%')
            </if>
            <if test="path != null and path != ''">
                or path like concat('%',#{path},'%')
            </if>
        </where>
    </select>
</mapper>